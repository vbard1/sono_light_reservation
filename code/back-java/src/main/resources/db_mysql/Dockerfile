FROM mysql:latest

ARG MYSQL_ROOT_PASSWORD
ARG MYSQL_DATABASE
ARG MYSQL_USER
ARG MYSQL_PASSWORD

ENV MYSQL_ROOT_PASSWORD='root'
ENV MYSQL_DATABASE='sonoLightReservation'
ENV MYSQL_USER='myuser'
ENV MYSQL_PASSWORD='mypassword'
ADD db_init.sql /docker-entrypoint-initdb.d/

ENV EXPOSE 3306
